import spain from '../images/spain.gif';

function Spain() {
  return (
    <div className="container">
      <img src={spain} alt="" className="map" />
    </div>
  );
}

export default Spain;
